[project]
name = "sport-rent-backend"
version = "0.1.0"
requires-python = ">=3.14"
dependencies = [
    "fastapi>=0.139.0",
    "sqlalchemy>=2.0.51",
    "uvicorn>=0.51.0",
]
[dependency-groups]
dev = [
    "ruff>=0.15.20",
]
